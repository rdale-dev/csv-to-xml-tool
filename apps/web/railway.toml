[build]
builder = "dockerfile"
dockerfilePath = "apps/web/Dockerfile"

[deploy]
startCommand = "sh -c 'npx prisma db push --skip-generate && node server.js'"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
